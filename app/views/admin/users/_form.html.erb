<%= simple_form_for [:admin, user] do |f| %>

    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :admin, label: "Create as admin?" %>
    <%= f.button :submit, class: "btn btn-success pull-right btn-lg" %>
<% end %>
